import React from 'react';
import { StyleSheet, View } from 'react-native';

const RenderMessage = (props) => {
  return <View {...props} style={{backgroundColor: 'red'}}></View>
};

export default RenderMessage;
